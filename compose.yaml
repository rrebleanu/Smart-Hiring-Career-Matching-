services:
  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: parola
      POSTGRES_DB: jobportal
      POSTGRES_USER: postgres
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: